server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    serviceUrl:
      defaultZone: http://service-register-a:8761/eureka/
    server:
      enableSelfPreservation: false
  instance:
    hostname: service-register-b